import Nerv from "nervjs";
import Taro from "@tarojs/taro-h5";
import { View } from '@tarojs/components';
import TaroRichTextNoWxParse from "../taro-rich-text-no-wxparse/index";
class XbRichTextComponent extends Taro.PureComponent {
  constructor(props) {
    super(props);
  }
  render() {
    const { richText, customStyle, type } = this.props;
    if (type === 'markdown') {
      return <TaroRichTextNoWxParse {...this.props} />;
    } else if (type === 'html') {
      return <View style={customStyle}>
          
          <wxparser rich-text={richText} />
        </View>;
    }
  }
  config = {
    usingComponents: {
      wxparser: 'plugin://wxparserPlugin/wxparser'
    }
  };
}
XbRichTextComponent.defaultProps = {
  richText: '',
  raw: true,
  rawMaxLength: 100,
  type: 'markdown',
  customStyle: '',
  onImageClick: () => {},
  onLinkClick: () => {}
};
export default XbRichTextComponent;